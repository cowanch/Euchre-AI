package bot;

public class MonteCarloSearch 
{
	// At the beginning of play, set up the player's with random hands
	// The player retains their own hand
	// If a card would be picked up by the dealer, they do so (set up algorithm for picking best card to dump)
}
